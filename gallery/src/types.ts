export enum ApplicationStateEnum {
  GRID = "GRID",
  MENU = "MENU",
}

export interface IUrls {
  raw: string;
  full: string;
  regular: string;
  small: string;
  thumb: string;
  small_s3: string;
}
